; Stores the buffer in the video card memory as; a texture, for a later use; Integer representing the texture unit; That will store the current screen contents (0 to 9)fTextureUnit 2; Clear the screen buffer after capturing? (1=yes, 0=no)fClearScreenBuffer 1; Clear the depth buffer after capturing? (1=yes, 0=no)fClearDepthBuffer 1